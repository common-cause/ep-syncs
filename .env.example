# Copy this file to .env and fill in your credentials.
# Never commit .env to git.
# All credential env vars follow the {CREDENTIAL_NAME}_PASSWORD pattern.

# BigQuery — full service account JSON, unquoted
# GCP project: proj-tmc-mem-com | SA: com-dbt@proj-tmc-mem-com.iam.gserviceaccount.com
BIGQUERY_CREDENTIALS_PASSWORD={"type":"service_account",...}

# Airtable personal access token
AIRTABLE_API_KEY_PASSWORD=pat...

# Protect the Vote (PTV) API — credential type TBD from sample script
# PTV_API_KEY_PASSWORD=...
